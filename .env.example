PORT=3000
DATA_BACKEND=memory
# DATABASE_URL is only required when DATA_BACKEND=postgres, but `bun test` always
# needs it — the integration test suite runs against a real Postgres 16 instance
# regardless of which backend you run the dev server with.
DATABASE_URL=postgres://user:password@localhost:5432/corrector_dev
